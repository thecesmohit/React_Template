import React from "react";
import { Typography, TypographyProps } from "@mui/material";

export const MuiTypography: React.FC<TypographyProps> = (props: TypographyProps) => (
    <Typography {...props}/>
)